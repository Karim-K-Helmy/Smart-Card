# dependencies
node_modules/

# environment variables (الحقيقيين فقط)
.env
.env.*.local
.env.local

# uploads: تجاهل كل الملفات ما عدا الـ .gitkeep والمجلدات الفارغة
uploads/*
!uploads/.gitkeep
!uploads/temp/
!uploads/temp/.gitkeep

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
Thumbs.db